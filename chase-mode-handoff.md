# 하츄핑 도망 모드 — 인수인계 문서

> **이 문서의 용도**
> 집이나 다른 PC에서 이 작업을 이어서 할 때, 이 파일 하나만 읽으면 지금까지 무엇을 왜 만들었는지, 다음에 무엇을 해야 하는지 알 수 있도록 쓴 문서입니다.
> 코드를 읽지 않아도 이해되도록, **용어는 처음 나올 때 풀어 쓰고, 설명은 실제로 일어나는 순서대로** 정리했습니다.
>
> 작성 시점: 2026-08-25 · 브랜치 `private/jaehoon` · 커밋 `c790f64`

---

## 1. 지금 상태를 한 문장으로

**팀의 숨바꼭질 데모 위에, 하츄핑이 실제 방의 지형을 따라 도망다니고 플레이어가 쫓아가 잡는 「도망 모드」를 새 페이지로 얹어 두었고, 갤럭시 S25에서 1차 실기기 테스트와 그에 따른 수정까지 끝난 상태입니다.**

---

## 2. 저장소와 브랜치 지도

저장소가 두 개입니다. 내용은 같고 소유자만 다릅니다.

| 이름 | 주소 | 역할 |
|---|---|---|
| `origin` | `cheoljun1986-a11y/miniproject_CV_ver02` | **팀 공용 저장소.** 팀원들이 함께 씀 |
| `fork` | `mchjh1019/miniproject_CV_ver02` | **내 개인 사본(fork).** 폰 접속용 웹 주소를 여기서 만듦 |

> **fork(포크)란**: 남의 저장소를 통째로 내 계정으로 복사해 온 것입니다. 원본과 연결은 유지되지만 내 마음대로 설정을 바꿀 수 있습니다. 여기서는 **GitHub Pages 설정**을 내 마음대로 하려고 만들었습니다.

브랜치는 다음과 같습니다.

| 브랜치 | 내용 |
|---|---|
| `main` | 팀 공용 결과물. **내 코드는 여기 올리지 않습니다** |
| `cj_develop` | 팀원의 개발 브랜치. 주기적으로 `main`에 합쳐짐 |
| **`private/jaehoon`** | **내 작업 전부.** 도망 모드, v3 지도뷰, 진단 페이지, 문서 |
| `private/jaehoon-chase` | 도망 모드를 만들던 임시 작업용 브랜치. 이미 `private/jaehoon`에 합쳐졌으므로 **더 이상 필요 없습니다** |
| `private/junsung`, `private/baencho/...` | 다른 팀원들의 개인 브랜치 |

### GitHub Pages 설정

> **GitHub Pages란**: 저장소 안의 HTML 파일을 그대로 웹사이트로 띄워 주는 GitHub의 무료 기능입니다. 폰에서 WebXR을 쓰려면 반드시 `https://` 주소여야 하기 때문에 이걸 씁니다.

내 fork의 Pages는 **`private/jaehoon` 브랜치**를 서비스하도록 설정해 두었습니다. 한 저장소는 브랜치를 딱 하나만 서비스할 수 있습니다.

폰에서 열 주소:

```
https://mchjh1019.github.io/miniproject_CV_ver02/v4-chase.html
```

주소창에 치면 `?occlusion=cpu`가 자동으로 붙으며 한 번 새로고침됩니다. 정상 동작입니다 — 도망 모드는 누적된 공간 지도가 있어야 하는데, 그 지도를 만드는 것이 CPU 깊이 모드이기 때문입니다.

---

## 3. 집에서 이어서 시작하는 방법

```bash
git clone https://github.com/mchjh1019/miniproject_CV_ver02.git
cd miniproject_CV_ver02
git checkout private/jaehoon

# 테스트 (Node.js만 있으면 됨. 별도 설치 불필요)
node --test "tests/*.test.mjs"
```

테스트는 **152개 전부 통과**하는 것이 정상입니다. 하나라도 깨지면 그 지점부터 보면 됩니다.

폰 확인은 위의 Pages 주소로 하면 되고, PC에서 코드를 고쳤다면 `git push` 후 1~2분 뒤 반영됩니다.

---

## 4. 도망 모드가 실제로 하는 일 — 시간 순서

폰에서 버튼을 누른 순간부터 하츄핑이 잡힐 때까지, 내부에서 일어나는 일을 순서대로 씁니다.

### 1단계 — 원점이 찍힌다

`START AR`을 누르는 순간, **폰이 있던 그 자리가 좌표계의 원점(0, 0, 0)** 이 됩니다. 이후 모든 좌표는 이 점을 기준으로 한 미터 단위 숫자입니다.

여기서 **아주 중요한 함정**이 하나 있습니다. 원점은 "바닥"이 아니라 **"세션을 시작할 때 폰을 들고 있던 높이"** 입니다. 보통 가슴~눈높이니까, **실제 바닥은 대개 원점보다 1.4m쯤 아래(y ≈ −1.4)** 에 있습니다. 이걸 착각해서 처음엔 바닥이 통째로 계산 범위 밖으로 잘려 나가는 버그가 있었습니다.

### 2단계 — 스캔하면서 방의 표면이 점으로 쌓인다

20초 스캔 동안, 그리고 그 이후에도 계속, 폰은 **깊이 지도(depth map)** 를 받습니다.

> **깊이 지도란**: 카메라 화면의 각 픽셀이 "카메라로부터 몇 미터 떨어져 있는가"를 담은 저해상도 이미지입니다. 대략 80×60 정도의 아주 작은 흑백 사진이라고 생각하면 됩니다.

이 깊이 값을 3차원 좌표로 되돌려 놓으면, 방의 표면에 점들이 스프레이처럼 뿌려집니다. 이 점들을 **복셀(voxel)** 로 정리합니다.

> **복셀이란**: 공간을 일정한 크기의 정육면체 격자로 잘라 놓고 "이 칸에 물체가 있다/없다"만 기록하는 방식입니다. 화면의 픽셀(정사각형)을 3차원으로 확장한 것이고, 이름도 volume + pixel 입니다.

노이즈를 걸러내려고 **같은 칸이 3번 이상 관측되어야** 비로소 "물체 있음"으로 확정합니다.

### 3단계 — 복셀을 "다닐 수 있는 격자"로 바꾼다

복셀 지도는 "여기 뭔가 있다"만 알려줍니다. 하츄핑을 움직이려면 **"여기 설 수 있는가, 여기서 저기로 갈 수 있는가"** 를 알아야 합니다. 그래서 복셀을 위에서 내려다본 **바둑판 격자**로 다시 정리합니다.

- 바둑판 한 칸의 크기: **가로세로 20cm**
- 각 칸마다 높이를 **10cm 두께의 층(slab, 슬랩)** 으로 잘라 64겹까지 기록

한 칸이 여러 개의 "설 수 있는 높이"를 가질 수 있습니다. 예를 들어 탁자가 있는 칸은 **바닥**과 **탁자 위** 두 개의 높이를 갖습니다. 이것이 하츄핑이 가구 위로 뛰어오를 수 있는 이유입니다.

이 격자는 스캔이 끝난 뒤에도 **계속 갱신**됩니다. 복셀 하나가 새로 확정될 때마다 그 칸만 고쳐 쓰기 때문에 비용이 거의 없습니다. (전체를 매번 다시 만드는 방식과 비교해 **110배 빠름**을 측정으로 확인했습니다.)

### 4단계 — 설 수 있는 높이를 고른다

어떤 층에 물체가 있고 그 **위쪽 50cm가 비어 있으면** 그 위에 설 수 있다고 판정합니다. 50cm는 하츄핑의 몸 높이입니다.

**여기서 천장 문제가 생겼습니다.** 천장은 기하학적으로 탁자와 완전히 똑같습니다 — 물체가 있는 얇은 층이고, 그 위(천장 너머)는 스캔된 적이 없으니 "비어 있음"으로 보입니다. 그래서 1차 테스트에서 하츄핑이 천장으로 뛰어올랐습니다.

둘을 가르는 유일한 정보는 **높이**뿐이라, 지금은 **"바닥에서 1.3m보다 높은 면은 설 수 있는 곳으로 치지 않는다"** 는 규칙을 넣었습니다.

그런데 1단계에서 말했듯 "바닥이 어디인지"를 미리 알 수 없습니다. 그래서 **관측 데이터에서 바닥을 추정**합니다: **가장 낮으면서, 충분히 많은 칸(8칸 이상)이 공유하는 층**을 바닥으로 봅니다. "충분히 많이"라는 조건을 넣은 이유는, 깊이 센서가 가끔 바닥보다 아래에 엉뚱한 점을 몇 개 찍는데 그걸 바닥으로 착각하면 상한선이 통째로 위로 밀려 천장이 다시 허용되기 때문입니다.

스캔이 진행되다가 진짜 바닥이 나중에 발견되면, 상한선이 다시 계산되고 이미 판정해 둔 칸들도 전부 다시 계산됩니다.

### 5단계 — 갈 수 **있는** 곳만 남긴다

여기서 또 하나의 함정이 있습니다. **높이 2m 벽의 윗면은 평평하고 위가 뚫려 있으니 "설 수 있는 곳"으로 판정됩니다.** 하지만 올라갈 방법이 없습니다.

그래서 하츄핑이 **현재 서 있는 자리에서 실제로 걸어서/뛰어서 닿을 수 있는 칸들만** 골라냅니다. 시작점에서 이웃 칸으로 계속 번져 나가는 방식(**flood fill**, 물감이 번지듯 퍼지는 탐색)으로 계산합니다.

운영자 뷰에서 **초록색 = 갈 수 있는 곳**, **주황색 = 설 수는 있지만 닿을 수 없는 곳**, **빨간색 = 벽/막힌 곳** 으로 색이 나뉘는 것이 이 결과입니다.

### 6단계 — 어디로 도망갈지 고른다

갈 수 있는 칸 전부에 점수를 매겨 가장 높은 곳을 목적지로 삼습니다. 점수 항목은 다음과 같습니다.

| 항목 | 방향 | 이유 |
|---|---|---|
| 플레이어에게서 먼 거리 | **+** (최대 8m까지만) | 도망이니까. 상한을 둔 이유는 항상 같은 구석으로만 달려가지 않게 하기 위해 |
| 최근 15초 안에 지나간 칸 | **−** (크게) | **제자리에서 뱅뱅 도는 것을 막는 핵심 항목** |
| 지금 가던 방향과 비슷한 쪽 | **+** | 갑자기 되돌아 뛰면 부자연스러움 |
| 6m를 넘는 먼 거리 | **−** | 너무 멀면 순간이동처럼 보임 |
| 가구 위 같은 높은 곳 | **+** | 재미 요소 |
| 약간의 무작위 | **+** | 매번 같은 선택을 하지 않도록 |

목적지는 **2~4초마다 다시 고릅니다.** 즉 도망 모드를 켠 순간의 지도로 고정되는 것이 아니라, **돌아다니며 새로 스캔되는 공간이 몇 초 안에 도망 경로에 반영**됩니다.

### 7단계 — 목적지까지의 경로를 찾는다

**A\*(에이스타)** 라는 표준 경로 탐색 알고리즘으로 격자 위의 길을 찾습니다.

> **A\* 란**: 미로에서 출구까지 가는 길을 찾을 때, "지금까지 온 거리 + 목적지까지 남은 직선거리"가 가장 작은 칸부터 먼저 살펴보는 방법입니다. 무작정 다 뒤지는 것보다 훨씬 빠릅니다.

중요한 것은 **경로가 반드시 이웃한 칸을 하나씩 밟는 형태로만 나온다**는 점입니다. 그래서 하츄핑이 벽을 통과하거나 방을 가로질러 순간이동하는 일이 원리적으로 불가능합니다.

높이 차이에 따라 이동이 세 가지로 나뉩니다.

| 높이 차 | 판정 |
|---|---|
| 15cm 이하 | 그냥 걷기 |
| 15cm ~ 70cm | **점프** (짧은 포물선을 그림) |
| 70cm 초과 | **아예 갈 수 없음** — 이것이 벽을 못 넘는 이유 |
| 아래로 1.2m 초과 | 갈 수 없음 (너무 높은 데서 떨어짐) |

점프는 일부러 비용을 조금 비싸게 매겨 두었습니다. 돌아가는 길이 있으면 걸어가고, 점프가 유일한 길일 때만 뜁니다.

### 8단계 — 속도

플레이어와의 거리에 따라 속도가 달라집니다. **1차 테스트에서 "너무 빠르다"는 피드백을 받아 전부 절반으로 낮췄습니다.**

| 플레이어와의 거리 | 1차 테스트 때 | 현재 |
|---|---|---|
| 1.2m 이내 | 0.95 m/s | **0.50 m/s** |
| 2m 이내 | 0.85 | **0.42** |
| 4m 이내 | 0.60 | **0.28** |
| 그보다 멀 때 | 0.25 | **0.12** |

원래 기준을 "사람이 걷는 속도 약 1.2m/s"로 잡은 것이 잘못이었습니다. **폰을 들고 화면을 보면서 걷는 속도는 그보다 훨씬 느립니다.**

멀리 있을 때 거의 서성이게 만든 이유는, 그렇지 않으면 방 반대편으로 사라져 추격 자체가 성립하지 않기 때문입니다.

### 9단계 — 검거 판정

**1차 테스트 후 규칙이 바뀌었습니다.** 원래는 `SCAN` 버튼을 누르고 있어야 했는데, 지금은 **거리와 조준만** 봅니다.

| 조건 | 값 |
|---|---|
| 거리 | **1.2m 이내** |
| 화면 중앙에서 벗어난 각도 | **20° 이내** |
| 유지 시간 | **5초** |

두 조건이 동시에 만족되는 동안 화면 아래 게이지가 찹니다. 조건이 깨지면 **0으로 초기화되지 않고 천천히 줄어듭니다.** AR 거리 측정값은 원래 흔들리고 가구 뒤로 잠깐 사라지기도 하는데, 그때마다 5초를 처음부터 다시 세면 게임이 고장난 것처럼 느껴지기 때문입니다.

게이지가 40%, 70%를 넘으면 하츄핑이 점점 느려집니다. 추격전이 끝없이 이어지지 않고 수렴하게 만드는 장치입니다.

하츄핑이 화면 밖으로 나가면 **분홍색 화살표**가 방향을 가리킵니다. 이게 없으면 놓친 뒤 게임이 멈춰 버립니다.

**거리를 50cm에서 1.2m로 바꾼 이유**: 50cm에서는 키 50cm짜리 모델이 카메라 화각(약 70°) 밖으로 밀려나 화면에 담기지 않고, AR 위치 오차(10~20cm)가 판정을 좌우해 버립니다.

### 10단계 — 운영자 뷰

게임 화면과 별개로, 방을 위에서 내려다보는 진단 화면입니다. 도망 모드용으로 세 가지를 추가했습니다.

- 다닐 수 있는 칸을 색칠한 타일 (초록/주황/빨강 — 5단계 참조)
- 하츄핑이 지금 가고 있는 **주황색 경로선**
- 하츄핑 위치 표시

**하츄핑이 안 움직일 때 원인을 찾는 순서**는 이렇습니다: 초록 타일이 있는가 → 주황 경로선이 그려지는가 → 실제로 움직이는가. 어디서 끊기는지 보면 원인이 바로 나옵니다.

---

## 5. 조정할 수 있는 숫자들

게임 느낌을 바꾸고 싶을 때 건드릴 값과 그 위치입니다.

| 무엇 | 현재 값 | 어디 |
|---|---|---|
| 도망 속도 (4단계) | 0.12~0.50 m/s | `src/chase-runner.js` 맨 위 |
| 검거 거리 | 1.2m | `src/capture-gauge.js` 맨 위 |
| 검거 각도 | 20° | 같은 파일 |
| 검거 유지 시간 | 5초 | 같은 파일 |
| 게이지 감소 속도 | 초당 12% | 같은 파일 |
| 격자 한 칸 크기 | 20cm | `src/config.js` |
| 하츄핑 몸 높이(머리 공간) | 50cm | 같은 파일 |
| 걷기/점프 경계 | 15cm | 같은 파일 |
| 점프 한계 | 70cm | 같은 파일 |
| **바닥 위 높이 상한 (천장 차단)** | **1.3m** | 같은 파일 |
| 목적지 재선택 주기 | 2~4초 | 같은 파일 |
| 도망 모드 시작에 필요한 최소 지도 | 120칸 | 같은 파일 |

---

## 6. 1차 실기기 테스트(갤럭시 S25) 결과와 수정 내역

| 발견된 문제 | 원인 | 조치 |
|---|---|---|
| 하츄핑이 너무 빠름 | 사람의 걷는 속도를 기준으로 잡았는데, 폰을 보며 걷는 속도는 훨씬 느림 | 속도 밴드 전부 절반으로 |
| **천장으로 뛰어오름** | 천장과 탁자가 기하학적으로 구별되지 않음 | 바닥 추정 + 바닥 위 1.3m 상한 |
| `SCAN`을 길게 누르면 글자가 드래그되고, 그 메뉴를 없애려 뒤로가기를 누르면 **Chrome이 종료됨** | Android가 길게 누르기에 텍스트 선택 도구모음을 띄움 | 검거에서 버튼 누르기 조건 제거 + HUD 전체 텍스트 선택 차단 |
| 버튼이 화면 맨 아래라 뒤로가기 키와 겹침 | Android 제스처 바/내비게이션 영역 침범 | 컨트롤 바를 64px + 안전영역만큼 위로 |
| 상단 회색 통계가 화면 절반을 가림 | 진단 정보를 항상 띄우고 있었음 | **기본 숨김 + 우측 상단 「수치」 토글** |

부수적으로 도망 모드 페이지에서는 「기준점 저장」·「복귀 오차 확인」 두 진단 버튼을 뺐습니다. 버튼이 많으면 컨트롤 바가 두 줄로 접혀 화면을 더 가리기 때문입니다.

---

## 7. 작업 중 발견해서 고친 팀 코드 버그

**앵커(anchor)가 실제로는 동작하지 않고 있었습니다.**

> **앵커란**: "이 좌표를 실제 공간의 이 지점에 붙여 놓아라"라고 AR 시스템에 부탁하는 기능입니다. 시간이 지나며 좌표가 조금씩 밀리는 현상(drift)을 시스템이 알아서 보정해 줍니다.

증상은 이렇습니다. 앵커가 보정한 새 위치를 화면상의 3D 객체에 적용할 때, **three.js가 매 프레임 그 값을 덮어써서 지워 버리고 있었습니다.** 그 결과 **게임 로직은 보정된 위치를 알고 있는데, 눈에 보이는 모델은 원래 자리에 그대로 서 있는** 상태였습니다.

수정했고 회귀 테스트도 넣어 두었습니다. **이 내용은 팀에도 공유해야 합니다** — 아직 전달하지 않았습니다.

---

## 8. 아직 안 된 것 / 알려진 한계

- **카메라 앞에 손을 흔들면 가려지지 않습니다.** 깊이 계산이 "장면은 정지해 있다"를 전제로 하고, 근거리(약 50cm 이내)에는 사각지대가 있으며, 해상도가 낮아 얇은 물체의 윤곽이 사라집니다. **고치기보다 측정된 한계로 문서화하는 편을 권합니다.**
- 숨는 위치가 "잘 안 보이는 곳"을 고르지 않습니다(가시성 점수 미구현).
- 후보 배치 점수가 "현재 위치에서 1~8m"를 사실상 강제해, 넓은 공간에서 캐릭터가 특정 영역에만 몰립니다.
- `plane-detection`(평면 인식), `light-estimation`(조명 추정) 미사용.
- 하츄핑 모델(`hcp.glb`)이 삼각형 186,000개로 과합니다. 줄이면 프레임이 개선됩니다.

---

## 9. 다음 작업 — 운영자 모드 수정본에 도망 모드 얹기

### 무엇이 달라졌나

내가 도망 모드를 만드는 동안, 팀이 **운영자 모드의 문제 두 가지를 고쳤습니다.**

| 팀의 수정 | 내용 |
|---|---|
| `24e578b` 지도가 비어 보이는 이유를 표시하고, 지도를 화면 안에 유지 | 원인이 "깊이가 아예 안 들어옴"인지 "점은 들어오는데 3회 관측을 못 채움"인지 구별할 수 있게 수치를 표시. 그리고 지도가 커질 때마다 **카메라가 자동으로 지도 전체를 담도록** 함 (운영자가 직접 드래그하면 자동 추적 중단) |
| `46d860d` 복셀이 전부 검게 그려지던 버그 | 재질 설정이 잘못되어 색이 0으로 곱해지고 있었음. 실제로는 복셀 2만 개가 방 윤곽까지 제대로 만들어지고 있었음 |

두 번째 것은 **꽤 중요합니다.** 그동안 운영자 뷰가 시커멓게 보이던 게 지도가 안 만들어져서가 아니라 단순한 색상 버그였다는 뜻입니다.

팀은 그 외에 **가위바위보 손동작 게임**도 추가했습니다(`main`에 병합됨). 도망 모드와 직접 충돌하지는 않지만 같은 파일들을 건드립니다.

### 합쳐야 하는 지점

내 도망 모드와 팀의 수정이 **같은 파일 네 개**를 건드립니다. 여기서 충돌이 납니다.

| 파일 | 내가 한 것 | 팀이 한 것 |
|---|---|---|
| `src/operator-view.js` | 다닐 수 있는 칸 타일, 경로선, 하츄핑 표시 추가 | 카메라 자동 프레이밍, 복셀 색상 버그 수정 |
| `src/ui.js` | 도망 모드 UI, 수치 토글, 버튼 방어 코드 | 운영자 상태 표시 추가, 가위바위보 UI |
| `src/main.js` | 도망 모드 상태 관리, 격자 갱신 | 자동 프레이밍 연결, 가위바위보 연결 |
| `src/config.js` | 도망 모드 상수 | 가위바위보 상수 |

### 권장 절차

1. 팀 최신 `main`을 받아 `private/jaehoon`에 병합한다.
2. 충돌 네 파일을 **양쪽 기능이 모두 살아남도록** 손으로 정리한다. 특히 `operator-view.js`는 팀의 복셀 색상 수정과 내 타일 렌더링이 **둘 다** 들어가야 한다.
3. 테스트를 돌린다 — 내 152개 + 팀이 추가한 테스트가 전부 통과해야 한다.
4. `v4-chase.html`에 팀이 `index.html`에 추가한 것 중 필요한 것이 있는지 확인한다. **단, `index.html` 자체는 절대 건드리지 않는다** — 팀 전체가 보는 데모 주소가 가리키는 파일이다.
5. 폰에서 운영자 뷰를 열어 **복셀이 검지 않게 나오는지**, 그리고 **내 초록 타일이 그 위에 함께 보이는지** 확인한다.

### 확인할 것

병합 후 운영자 뷰에서 다음 세 가지가 **동시에** 보여야 성공입니다.

- 방 윤곽을 이루는 **색이 있는 복셀** (팀의 수정)
- 그 위에 겹친 **초록/주황/빨강 타일** (내 도망 모드)
- 하츄핑의 **주황색 경로선**

---

## 10. 파일 구성 요약

도망 모드와 관련해 새로 만든 것들입니다.

| 파일 | 역할 |
|---|---|
| `v4-chase.html` | **도망 모드 페이지.** 폰에서 여는 주소 |
| `src/traversal-grid.js` | 복셀 → 다닐 수 있는 격자 (3~5단계) |
| `src/chase-path.js` | 목적지 선택과 경로 찾기 (6~7단계) |
| `src/chase-runner.js` | 실제로 걷고 뛰게 만드는 부분 (7~8단계) |
| `src/capture-gauge.js` | 검거 판정 (9단계) |
| `tests/traversal-grid.test.mjs` 외 3개 | 위 네 개에 대한 테스트 |
| `probe-capabilities.html` | 기기가 어떤 AR 기능을 지원하는지 확인하는 진단 페이지 |
| `v3-mapview.html` | 수집된 후보점 분포를 눈으로 보는 진단 페이지 |

기존 문서 `webxr_hide_seek_development_handoff.md`에는 프로젝트 전체의 배경, 초기 설계, 로드맵, 넓은 공간에서의 문제 분석이 들어 있습니다. **이 문서는 그 뒤에 이어지는 도망 모드 부분만** 다룹니다.
