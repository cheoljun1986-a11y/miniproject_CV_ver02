# 도망 모드: 헛가림·순간이동 수정 노트 (2026-08-27)

이 문서는 커밋 3개(검거 사거리 2m 정정 → 실측 7점 가림 판정 → 지도 anchor 갱신 연결)의
**무엇을, 왜, 어떤 원리로** 바꿨는지를 코드를 안 연 사람도 따라올 수 있게 정리한 것이다.
다음 작업 세션(사람이든 AI든)이 이 시스템을 잘못 추측하지 않도록, 검증된 사실과
아직 기기에서 확인 안 된 것을 구분해 둔다.

---

## 0. 먼저 용어

| 용어 | 뜻 |
|---|---|
| **월드 좌표** | ARCore가 주는 원시 좌표. 세션 시작 지점이 원점. ARCore가 자기 위치 추정을 보정하면 이 좌표계 전체의 의미가 한 번에 바뀐다 |
| **맵 좌표** | 복셀 지도·격자·하츄핑 위치를 저장하는 좌표. 지도 anchor의 자세를 기준으로 월드와 상호 변환된다 (`toMapSpace` / `toRenderSpace`) |
| **pose(포즈)** | 위치 + 방향을 묶은 "자세". 위치만이 아니라 회전 포함 |
| **anchor(앵커)** | ARCore에 "이 지점을 계속 추적해줘"라고 등록한 좌표. 눈으로 비추는 표식이 아니다. ARCore가 내부 지도를 보정할 때마다 등록된 앵커의 좌표값을 새로 알려준다 |
| **드리프트 보정** | 흰 벽 등을 보는 동안 쌓인 위치 추정 오차를, 특징 있는 물체를 다시 알아본 순간 한 번에 고치는 것(루프 클로저). 이때 월드 좌표가 계단식으로 튄다 |
| **화면 가림** | 실시간 깊이 측정으로 하츄핑이 현실 물체에 가려 보이는 것. 눈에 보이는 효과 |
| **맵 가림** | 저장된 복셀 지도 위에서 직선을 그어 계산하는 가림. 검거 게이지 속도에만 쓰였다 |
| **7점 판정** | 몸을 픽셀로 자르지 않고, 실루엣 위 7개 지점(머리·발·중심·양 옆구리·어깨 2)의 가림 여부로 "몇 % 가려졌나"를 재는 방식 |

---

## 1. 페이지·모드 매트릭스 — 추측 금지, 여기서 확인

이 저장소는 **페이지와 URL 파라미터에 따라 도는 모듈이 완전히 다르다.**
이걸 뭉뚱그려 추측한 것이 이전 분석 오류들의 공통 원인이었다.

| | `v4-chase.html` (도망 모드) | `app.html` (숨바꼭질/진단) |
|---|---|---|
| 진입 시 | 파라미터 없으면 `?occlusion=cpu`로 자동 리다이렉트 | 파라미터별 모드 (`?voxel=debug`, `?depth=cloud` 등) |
| 게임 흐름 | **맵 생성 → 종료 = 즉시 추격 시작.** 숨바꼭질 단계 없음 (`freezeMap()`이 `startChase()` 직접 호출) | 스캔 → 숨기기 → 찾기 (숨바꼭질 있음) |
| 전체 화면 가림 그물 (`CpuDepthOccluder`) | **이제 생성 안 함** (커밋 2) — 깊이 읽기 자체는 유지 | `?occlusion=cpu`로 열면 생성 |
| 하츄핑 가림 표현 | 7점 실측 → 불투명도 (0.25~1) | 깊이 버퍼로 픽셀 단위 가림 |
| 지도 anchor (`MapAnchor`) | 있음, 이제 매 프레임 갱신 (커밋 3) | 없음 (null → 변환은 항등) |
| 하츄핑 전용 anchor | 배치 시 사용, **추격 시작하면 무력화** (`externalControl`) | 숨긴 위치 유지에 사용 |

주의: 운영자 카드의 "고정 anchor"는 **하츄핑 전용 anchor**, "지도앵커"는 **지도 anchor**다.
이름만 같은 다른 물건.

---

## 2. 커밋 1 — 검거 사거리를 지정값 2m로 정정

- **문제**: 직전 커밋이 사거리를 1.2m → 3.0m로 올렸는데, 지정된 값은 2m였고 그 지시가
  코드 어디에도 기록되지 않았다. 인수인계 문서는 여전히 1.2m라고 적혀 있어
  코드·문서·지시가 셋 다 달랐다.
- **수정**: `src/capture-gauge.js`의 `CAPTURE_RADIUS_M = 2.0` + 주석에 지정값임을 명기.
  `chase-mode-handoff.md`의 1.2m 세 곳 갱신. 화면 안내문은 이미 상수를 참조하므로 자동 반영.
- **기대효과**: 2m 안에서 게이지가 찬다. 테스트의 사거리 밖/안 수치(4~6m / 0.5~0.8m)는
  2m에서도 그대로 성립해 로직 변화 없음.

---

## 3. 커밋 2 — 가림을 실측 7점 판정으로 (핵심 수정)

### 문제가 무엇이었나

하츄핑이 **현실에 가릴 물건이 없는데도 화면에서 통째로 사라졌다.**
20cm 크기 축소 후 더 심해졌다.

### 원인 (코드로 확인된 것)

사라지게 만든 주체는 **전체 화면 가림 그물**이다. 화면을 80×66 격자로 나눠 각 점의
깊이를 재고, 그 모양대로 "색은 안 칠하고 깊이만 기록하는" 보이지 않는 판을 만들어
하츄핑을 픽셀 단위로 잘라냈다. 이 방식은 두 지점에서 무너진다:

1. **해상도 대비 몸이 작다.** 2m 거리에서 20cm 몸은 격자 몇 칸. 노이즈 한 칸이
   몸의 큰 조각을 지운다.
2. **그물이 66ms마다 갱신**되는데 화면은 더 빠르다. 폰을 빠르게 돌리면 그물이 옛
   위치에 남아, 실제 벽보다 밀린 자리에서 하츄핑을 자른다.

근본적으로, ARCore 깊이 오차는 거리에 비례해 커져서 2~3m에서는 수 cm~십수 cm에
달한다. **오차가 캐릭터 크기와 같은 자릿수라 픽셀 단위 자르기는 성립하지 않는다.**
격자를 아무리 촘촘히 해도 측정 자체가 틀리는 건 못 고친다.

직전 커밋(63a4bc3)의 반투명 유령은 **저장 지도 기반 판정**(`capture.visible`)에
물려 있었는데, 실제로 지우는 주체는 **실시간 그물**이라 신호가 달랐다. 그래서 정작
사라진 순간에 유령이 안 켜졌다.

### 무엇으로 바꿨나

**몸을 하나의 물체로 판정한다.** 새 모듈 `src/live-visibility.js`:

1. 하츄핑 실루엣 위 7점(기존 `line-of-sight.js`의 패턴을 공유)을 실시간 깊이 영상에 투영
2. 각 점에서 "측정된 깊이가 그 점보다 25cm 이상 가까우면" 가려진 것으로 셈
   (25cm 여유는 거리 오차 흡수용 — 오차는 이보다 작고, 진짜 가구는 이보다 깊다)
3. 가려진 비율 → `CaptureGauge`의 visibility 입력 → `visibleScale`(0.25~1)
4. **그 한 숫자가 게이지 배속이자 모델 불투명도다.** `setTargetOpacity(capture.visibleScale)`

| 상황 | 이전 | 이후 |
|---|---|---|
| 노이즈 한 칸 | 몸 조각이 통째로 사라짐 | 불투명도 1/7만 흐려짐 |
| 진짜 벽 뒤 | 픽셀 단위로 잘림 | 0.25 실루엣만 남음 — **무엇이 가렸는지 보임 (디버깅 요구)** |
| 화면 밖 / 깊이 없음 | (그물 상태에 따라 제각각) | "측정 불가" = 안 가림. 데이터 없음은 절대 벌점 아님 |
| 지도가 어긋난 상태 | 지도 기반 게이지가 헛가림 | 판정이 렌더 좌표(카메라 실좌표 vs 그려진 위치)라 **지도 표류 무관** |

전체 화면 그물은 도망 페이지에서 **생성 자체를 중단**했다 (`main.js`의
`CPU_OCCLUSION_MODE && !ui.hasChaseControls()` 게이트). 깊이 **읽기**는 그대로다 —
복셀 지도와 7점 판정이 계속 쓴다. 프레임당 깊이 샘플: 4,800회 → 7회.

이진 유령 경로(`setTargetGhost`/`setNinjaGhost`)는 대체되어 삭제했다.
하츄핑 자체의 depthTest는 켠 채다(자기 몸 앞뒤 정렬용) — 현실 물체는 이제 깊이를
안 쓰므로 잘리지 않는다.

### 트레이드오프 (알고 받아들인 것)

- 문틀에 반쯤 걸치면 "반 잘림"이 아니라 "전체가 반투명"으로 보인다. 20cm 캐릭터라
  구분이 거의 안 되고, 노이즈에 토막나는 것보다 낫다고 판단.
- 진짜로 숨어도 0.25 실루엣이 남는다. 추격 게임이라 놓치는 것보다 낫고,
  디버깅("무엇이 가렸나 판단")을 위해 의도된 동작.
- 얇은 물체(의자 다리)는 7점 중 소수만 가려 약하게만 반영된다.

### 새 테스트가 잡은 실제 버그

`invertRigidMat4`(강체 변환 역행렬)를 처음에 전치를 두 번 해서 원래 회전을 돌려주게
잘못 썼고, 왕복 테스트가 즉시 잡았다. **이 모듈의 수학은 전부 순수 함수라
`node --test`로 검증된다** — 기기 없이 못 잡는 부분은 아래 6절에만 남는다.

---

## 4. 커밋 3 — 지도 anchor를 실제로 갱신

### 문제가 무엇이었나

하츄핑이 어느 순간 **엉뚱한 현실 위치로 순간이동**하는데, 운영자 모드에서는
궤적이 **완전히 연속**이었다.

### 원인 (코드로 확인된 것)

지도 전체를 현실에 못박는 `MapAnchor`가 있고 파일 주석에 정확히 이 문제를 막으려고
만들었다고 적혀 있는데, **매 프레임 포즈를 물어보는 `update()`가 소스 어디에서도
호출되지 않았다.** 생성·리셋·예약(`beginTracking`)·변환 요청만 있었다. 도입 커밋
(2a50ca5)부터 그랬다 — 머지 사고가 아니라 처음부터 미연결.

결과: 변환이 영원히 항등 → 맵 좌표 ≡ 월드 좌표 → 드리프트 보정이 일어나면
지도·하츄핑은 옛 좌표에 남고 현실만 어긋난다.

- **순간이동**: 저장된 숫자는 안 변했는데 그 숫자가 가리키는 현실 지점이 바뀜
- **운영자 모드는 연속**: 저장된 숫자를 그리니까. 당연한 결과이며, 그래서 이 버그는
  운영자 모드로는 관찰이 불가능했다
- **"흰 벽 → 가구 비추면 나만 튄다"는 기존 관찰**: 같은 원인의 다른 단면.
  플레이어 점은 월드 좌표(방금 보정됨), 지도는 저장 좌표(안 움직임)
- 헛가림과의 시간적 연관: 보정은 트래킹이 나쁠 때(빠른 회전, 밋밋한 벽) 일어나므로
  하츄핑을 놓치는 순간과 겹친다. 인과가 아니라 공통 원인

### 무엇으로 바꿨나

1. 렌더 루프(`SPACE_MAPPING_MODE` 블록 첫머리)에서 `mapAnchor.update(frame, localSpace)`
   매 프레임 호출. 이 호출이 첫 가능한 프레임에 앵커를 실제로 만들고, 이후 매 프레임
   최신 포즈를 받아온다. 상태 변화는 도망 기록에 `map-anchor` 이벤트로 남는다
2. 쓰기만 되고 읽히지 않던 `mapAnchorState`를 운영자 카드에 표시:
   **지도앵커 O / 생성 중 / 일시 손실 / 미지원**
3. **운영자 뷰 좌표 통일**: 하츄핑·타일은 맵 좌표인데 플레이어 점·경로만 월드 좌표로
   한 화면에 섞여 있었다. 앵커가 살아나는 순간 이게 실버그가 되므로(보정 때 플레이어
   점이 튀어 보임) 전부 맵 좌표로 변환. 복셀 배경만 원좌표 유지(수만 점 재투영 비용
   대비 진단 가치 없음 — 코드 주석 참조)

### 기대효과

보정이 일어나면 **지도·하츄핑·격자가 통째로 방을 따라간다.** 순간이동 소멸.
기기가 anchors 미지원이면 이전과 동일하게 동작하고(항등), 이제 그 사실이
"지도앵커 미지원"으로 화면에 보인다.

### 한계 (알고 있는 것)

- 앵커는 드리프트를 없애는 게 아니라 지도가 ARCore의 최신 추정을 **따라가게** 한다.
  절대 정확도가 아니라 "화면과 지도의 일치"를 지키는 장치다
- 앵커 하나는 통짜 이동·회전만 보정한다. 방 하나 크기에선 충분; 넓은 공간은 다중 앵커 필요

---

## 5. 검증된 사실 (다음 세션은 여기서 시작할 것)

전부 실제 소스를 읽고 확인했다. 추측이 아니다.

| 사실 | 위치 |
|---|---|
| 도망 페이지는 `?occlusion=cpu` 강제 | `v4-chase.html` body 첫 스크립트 |
| 맵 생성 종료 = 즉시 추격 (숨바꼭질 없음) | `main.js` `freezeMap()` |
| 하츄핑 전용 anchor는 추격 중 무력화 | `ninja-game.js` `updateTargetAnchor()` 첫 줄 |
| 게이지 visibility 입력·hint·0.25 바닥 | `capture-gauge.js` (`visibleScale`이 그 바닥) |
| 7점 실루엣 패턴의 원본 | `line-of-sight.js` `bodySampleOffsets()` |
| 깊이 샘플 ↔ 월드 좌표 변환 규약 (u,v 좌상단 원점, -Z 수직거리) | `depth-math.js` `depthSampleToWorld()` — `live-visibility.js`의 `projectToView()`가 그 정확한 역 |
| 깊이 스냅샷은 프레임당 1회 캐시 공유 | `cpu-depth-frame-source.js` |
| 테스트 실행 | `node --test tests/*.test.mjs` (package.json 없음, 의존성 없음). 현재 371개 |

## 6. 기기에서만 확인 가능한 것 (내일 회사에서 볼 것)

1. **순간이동 소멸 여부.** 흰 벽을 한참 보다가 가구를 비추는 재현 절차로.
   운영자 카드의 "지도앵커"가 O인지 먼저 확인 — **미지원이면 커밋 3은 무효**(안전하게
   이전 동작)이고 다른 접근 필요
2. **헛가림 소멸 여부.** 빈 방향을 겨눴을 때 하츄핑이 선명하게 유지되는지, 진짜 가구
   뒤에서 0.25 실루엣이 남는지, 실루엣 위로 가린 물체가 보이는지
3. **도망 기록**(수치 카드 하단)에 `map-anchor` 이벤트가 찍히는 시점과 순간이동
   느낌이 사라졌는지의 대응
4. 폰을 빠르게 돌릴 때 하츄핑 가장자리가 더 이상 뜯기지 않는지 (그물 지연 소멸 확인)
5. 깊이 원본 해상도는 코드가 읽지 않아 미확인 — 필요하면
   `depthInformation.width/height` 로그 한 줄

## 7. 되돌리고 싶을 때

커밋이 독립적이라 개별 revert 가능:
- 사거리만: `capture-gauge.js`의 상수 하나
- 가림 방식만: 커밋 2 revert (그물 방식 복귀 — 단, 유령은 여전히 잘못된 신호에 물려 있음)
- 앵커만: 커밋 3 revert (순간이동 복귀)
