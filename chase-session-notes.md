# 하츄핑 술래잡기 — 작업 정리

이 문서는 도망(술래잡기) 모드를 실기기에서 시험하고 고쳐 나간 과정을 정리한 것이다.
코드를 열지 않고도 읽을 수 있게 썼고, 전문 용어는 처음 나올 때 풀어 두었다.

- 대상 브랜치: `private/jaehoon` (개인 fork `mchjh1019`)
- 시험 주소: <https://mchjh1019.github.io/miniproject_CV_ver02/v4-chase.html>
- 이 문서 시점의 상태: 테스트 353개 전부 통과, 팀 `main` 에는 아직 반영하지 않음

---

## 1. 지금 어떻게 동작하는가

조작이 세 단계로 줄었다. 켜고 끄는 선택지는 없다 — 이 페이지는 술래잡기 전용이다.

1. **START AR**
2. **[맵 생성]** 을 누르고 방을 천천히 걸으며 비춘다.
   상태줄에 `복셀 N · 설 수 있는 칸 M` 이 실시간으로 올라가므로, 언제 끝낼지를 숫자를 보고 판단한다.
3. **[맵 생성 종료]** 를 누르면 지도가 **동결**되고, 그 즉시 하츄핑이 나타나 도망친다.

도망 중에는 **[하츄핑 재생성]** 버튼이 나타난다. 지도는 그대로 두고 하츄핑만 새 자리에 놓는다.
검거에 성공하면 라운드가 끝나고 **[맵 다시 만들기]** 만 남는다.

---

## 2. 공간을 인식하는 방식 — 두 개의 격자

크기가 다른 격자 두 개가 서로 다른 일을 한다. 이 구분이 이 프로젝트 전체의 기초다.

| | 크기 | 답하는 질문 |
|---|---|---|
| **복셀(voxel)** | 5cm 정육면체 | "여기 물체가 있다" |
| **이동 칸** | 20 × 20cm 바닥 타일 | "여기 발을 디딜 수 있다" |

### 시간 순서로 보면

1. **원점이 정해진다.** AR 세션이 시작될 때 폰이 있던 자리다.
   그 순간 공간 전체가 원점 기준 20cm 간격으로 썰린다.
   미리 만들어 저장하는 것이 아니라, 좌표를 나누기만 하면 "몇 번 칸"인지 나오는 규칙이다.
2. **깊이 카메라가 표면까지의 거리를 잰다.** 그 지점이 속한 5cm 복셀이 "찼음"으로 기록된다.
   같은 복셀이 **3번 이상** 관측되어야 진짜 물체로 인정한다. 한 번만 찍힌 것은 노이즈일 확률이 높다.
3. **복셀이 확정되면 이동 격자에 통보된다.** 칸 하나는 20 × 20cm 기둥이고,
   그것을 10cm씩 층으로 썰어 "몇 층이 찼는지"만 기억한다.
4. **설 수 있는지 판정한다.** 어떤 층이 차 있고 그 위로 몸 높이(34cm)가 비어 있으면 설 수 있다.
   하츄핑은 그 **층의 윗면**에 선다.

### 여기서 나오는 성질들

- **칸은 복셀이 들어올 때 비로소 생겨난다.** 한 번도 안 비춘 공간에는 칸 자체가 없으므로,
  하츄핑은 스캔하지 않은 곳으로 갈 수 없다. "안 가는" 것이 아니라 경로 탐색에서 보이지 않는다.
- **아래층 무더기는 필요 없다.** 공중에 복셀만 있어도 발판이 된다.
  이는 의도한 것이다 — 책상 상판이 정확히 그 모양이고, 아래층을 요구하면 가구에 올라갈 수 없다.
- **발판은 증거를 요구하고 머리 위는 요구하지 않는다.** 밑은 실제로 관측된 복셀이어야 하지만,
  위쪽은 "안 비춘 곳"도 "비었다"로 친다. 이 비대칭이 벽 중턱을 선반처럼 보이게 만드는 원인이며,
  그래서 "바닥에서 1.3m 위까지만" 이라는 높이 상한을 따로 둔다.

---

## 3. 실기기 시험에서 드러난 문제와 조치

### 3-1. 하츄핑이 바닥에 내려오지 않고 책상 높이로만 다녔다

원인이 셋 겹쳤고, 가장 큰 것은 **이웃 판정 자체**였다.

옆 칸으로 갈 때 **현재 높이와 가장 가까운 층 하나만** 후보로 냈다. 책상 위에 있으면
옆 칸의 바닥으로 가는 길이 **아예 생성되지 않았다.** 내려가기 싫었던 것이 아니라
내려가는 선택지가 없었다.

여기에 두 가지가 더해졌다. 오를 수 있는 높이를 0.7m로 잡아 바닥→의자(0.45m)→책상(0.73m)이
계단처럼 이어졌고, 도망 목적지 점수가 높은 곳에 가산점을 주고 있었다.

**조치**

- 옆 칸의 **모든 높이를 각각 후보**로 내고, 대신 가격을 다르게 물린다.
  오르는 점프는 높이의 2배, 내려오기는 절반, 그리고 바닥 위 고도에 비례한 통행료를 걸음마다 부과한다.
  → 책상-의자-책상 경로가 책상-**바닥**-책상보다 항상 비싸진다. 금지가 아니라 가격이라 필요하면 여전히 오른다.
- 오르기 상한 0.7m → **0.45m**. 의자는 되고 바닥→책상 직행은 불가.
- 목적지 점수의 "높은 곳 가산점"을 **감점**으로 뒤집음.

### 3-2. 발밑에서 사라졌다가 벽 근처에서 나타났다

영상 33.5초 지점이 증거였다. 화면에는 바닥과 본인 신발만 보이는데
HUD 는 `검거 중 · 1.1m` 를 표시하고 있었다. 조준선 안에 분명히 있는데 그려지지 않은 것이다.

이 페이지는 가림 기능을 켜고 있어, **실제 세계보다 뒤에 있는 물체는 그려지지 않는다.**
즉 하츄핑이 실제 바닥보다 아래에 있었다. 바닥 높이를 데이터에서 추정하는데,
드리프트로 위치가 밀리면 실제 바닥보다 아래에 가짜 층이 생기고,
그것이 8칸을 넘으면 코드가 그것을 바닥이라고 믿는다.

### 3-3. 벽을 넘어 복도로 나가 있었다

두 사건이 서로 다른 원인이었다.

- **벽면 1m 높이에 떠 있던 것** — 3-2와 같은 뿌리다. 카메라를 위로 들지 않으면
  벽의 아래쪽만 찍히고, 찍힌 것 중 가장 높은 지점 위가 "빈 공간"이 되어 선반처럼 읽힌다.
- **복도에 나가 있던 것** — 유리 때문이다. 영상 28초에 방 오른쪽에 유리 칸막이가 보인다.
  **깊이 카메라는 유리를 통과한다.** 적외선이 유리를 지나 복도 바닥에 부딪혀 돌아오므로,
  지도에는 방과 복도가 뻥 뚫린 하나의 공간으로 기록된다. 유리가 있다는 사실 자체가 데이터에 남지 않는다.
  **복도로 걸어가지 않았어도, 방에서 유리 쪽을 비추는 순간 복도가 지도에 들어왔다.**
  하츄핑 입장에서는 벽을 넘은 것이 아니라 열린 길을 걸어간 것이다.

이것은 센서의 한계이며 근본 해결이 어렵다. 완화책은 "플레이어가 실제로 걸어간 적 없는 구역은
제외" 정도인데, 아직 넣지 않았다.

### 3-4. 페이지가 "WebXR 확인 중…" 에서 멈췄다

보안 환경이나 라이브러리 차단이 아니었다. **`main.js` 에 닫는 중괄호 하나가 빠져 있었다.**
자바스크립트는 파일 하나에 문법 오류가 있으면 그 파일을 한 줄도 실행하지 않는다.
그래서 HTML 에 처음부터 적혀 있던 문구가 그대로 남았고, 다른 모듈은 요청조차 되지 않았다.

**왜 테스트가 못 잡았나** — `main.js` 는 부품을 연결하는 배선 계층이라 단위 테스트가 없었고,
문법 검사에 쓰던 도구가 파일 확장자만 보고 **옛 방식(CommonJS)으로 해석**하는데
그 방식에서는 같은 코드가 정상으로 통과한다. 검사는 통과하는데 브라우저에서는 죽는 상태였다.

**조치** — `src/` 의 모든 파일을 **브라우저와 같은 방식으로** 파싱하는 검사를 추가했다.
일부러 괄호를 다시 지워 실패하는 것과, 되돌려 통과하는 것을 확인했다.

### 3-5. 점 하나로 발판이 생겼다

층 하나는 20 × 20 × 10cm 이고 5cm 복셀 32칸이 들어간다. 그중 **하나만** 차면
20 × 20cm 발판이 통째로 생겼다. 허공의 노이즈 한 점이 설 자리를 만들었다는 뜻이다.

실측하면 이렇다.

| 대상 | 층에 남는 복셀 수 |
|---|---|
| 평평한 바닥 한 칸을 촘촘히 관측 | 16 / 32 |
| 책상 상판이 칸에 1/4 걸침 | 4 / 32 |
| 가는 다리 하나 | 2 / 32 |

**조치** — 발판에 **복셀 4개**를 요구한다. 칸이 아니라 **층 단위**로 센다.
실제 표면은 여유롭게 통과하고 고립된 노이즈는 걸러진다.
다만 상판이 칸에 살짝 걸치면 정확히 경계선이므로, 실기기에서 지도에 구멍이 생기면
숫자 하나(`minSlabVoxels`)만 낮추면 되도록 조절 가능하게 두었다.

### 3-6. 너무 가까이 가야 검거가 됐다

검거 반경이 1.2m 라 거의 껴안아야 게이지가 움직였고, 추격이 몸싸움이 됐다.
**3.0m 로 넓혔다.** 화면 안내 문구는 상수를 그대로 쓰게 바꿨다 —
예전에 하드코딩된 "1.2m" 가 상수보다 오래 살아남은 적이 있어 테스트로 막아 두었다.

### 3-7. 조금만 가려도 하츄핑이 사라지고 검거가 멈췄다

지형 격자가 20cm 이고 하츄핑도 20cm 라, 복원 노이즈 **한 칸이 대상을 완전히 가린다.**
그때 아무것도 안 보이고 게이지까지 멈추면 오검출이 "게임이 멈춘 것"처럼 보인다.

**조치 두 가지**

- **완전히 가려져도 정상 속도의 25%로는 찬다.** 힌트는 `가려짐 — 매우 느림`.
- **가려진 동안 모델이 반투명으로 비친다.** 가림은 깊이 정보로 픽셀을 버리는 방식이라
  "가려진 부위만 반투명" 이라는 중간 상태를 만들 수 없다. 대신 가려진 동안
  모델 전체의 깊이 검사를 끄고 불투명도를 0.5로 낮춘다. 장애물 너머로 유령처럼 비쳐
  위치를 잃지 않는다.

---

## 4. 구조 변경 두 가지

### 4-1. 지도를 미리 만들고 동결한다

교수님 피드백 — *실시간으로 지도를 만드는 구조 자체가 잘못됐다. 오차가 쌓일 수밖에 없다.*

게임 중에도 지도가 계속 자라면, 오차가 지도 안에 누적된다. 그래서 지도는 게임 전에 만들고,
만든 뒤에는 건드리지 않는다. **[맵 생성]** 과 **[맵 생성 종료]** 가 그 경계다.

동결이 공짜로 없애는 문제들:

- 발밑에서 지형이 갈아끼워져 하츄핑이 얼어붙는 일 (게임 중 지도 불변)
- 같은 벽이 두 자리에 생기는 유령 벽 (게임 중 재관측 없음)
- 바닥 추정이 게임 도중 흔들리는 일 (동결 시점에 확정)

**다시 만들기는 이전 지도를 통째로 버린다.** 두 번의 산책을 섞으면 이 구조가 없애려는
누적 오차가 그대로 재현되기 때문이다.

함께 없앤 것: 20초 타이머, `+20초 스캔` 버튼, 조준점으로 숨을 자리를 한 점씩 찍는 방식.
숨을 자리는 이제 **동결된 지도의 "설 수 있는 칸" 전체**에서 뽑는다.

### 4-2. 지도를 원점 anchor 에 매단다

**anchor 는 좌표를 저장하는 것이 아니라 실제 공간에 못을 박는 것이다.**

| | 비유 | 결과 |
|---|---|---|
| 원점 기준 좌표 | 종이에 "문에서 12걸음" 이라 적어 둠 | 걸음 길이 추정이 틀어지면 메모가 통째로 틀림 |
| anchor | 벽에 실제로 스티커를 붙임 | 추정이 틀려도 스티커는 그 자리에 있음 |

ARCore 는 anchor 를 만들 때 주변의 실제 특징점을 함께 기억하고,
**매 프레임 "그 장소가 지금 좌표계에서 어디인지" 를 다시 계산해서 알려준다.**

#### 왜 필요한가 — 시간 순서

1. 지도를 시작할 때 원점 자리에 못을 박는다.
2. 이후 모든 저장을 **anchor 기준**으로 한다.
3. 복도로 나간다. 유리와 매끈한 벽에는 특징점이 거의 없어 카메라 추적이 무력해지고,
   관성 센서에만 의존하게 되어 오차가 급격히 쌓인다(**드리프트**).
   이때 anchor 도 지도도 **함께** 밀리므로 서로 간의 관계는 깨지지 않는다.
4. 방으로 돌아오면 ARCore 가 기억해 둔 특징점을 다시 보고 **위치를 한 번에 보정한다.**
   화면에서 궤적이 "팍" 튀는 순간은 오류가 생긴 순간이 아니라 **쌓인 오류가 수정되는 순간**이다.
5. ARCore 가 **anchor 의 새 좌표를 알려준다.**
6. 우리 복셀은 전부 "anchor 에서 얼마" 형태이므로 **지도 전체가 변환 하나로 따라온다.**
   복셀 8만 개를 고치는 것이 아니라 숫자 하나가 바뀐다.

anchor 가 없거나 미지원이면 두 변환 모두 항등이라 기존과 똑같이 동작한다.
추적을 잃으면 마지막 포즈를 유지한다 — 몇 프레임 항등으로 튕기는 것보다 낡은 변환이 낫다.
상태 변화는 도망 기록에 `지도 anchor` 로 남는다.

**한계** — anchor 하나로는 "지도가 통째로 밀리는 것" 은 잡히지만
"방 안에서 서서히 휘는 것" 은 잡히지 않는다. 그것은 anchor 를 여러 개 심어야 하며
이 데모 규모에는 과하다고 판단했다. 관측된 증상은 통째로 밀리는 쪽이었다.

---

## 5. 현재 튜닝 값

| 항목 | 값 | 의미 |
|---|---|---|
| 복셀 크기 | 5cm | 물체 모양의 최소 단위 |
| 복셀 확정 조건 | 관측 3회 | 노이즈 1차 방어선 |
| 복셀 상한 | 80,000 | 도달 시 상태줄에 경고 |
| 이동 칸 | 20 × 20cm | 발자국 하나 = 하츄핑 폭과 동일 |
| 높이 층 | 10cm | 발판 높이 오차의 원인 |
| **발판 조건** | **층당 복셀 4개** | 노이즈 2차 방어선 |
| 하츄핑 크기 | 20 × 20 × 34cm | 폭이 칸 하나와 일치 |
| 머리 위 여유 | 34cm (실제 검사 40cm) | 층 단위 올림 |
| 걸어 오를 수 있는 높이 | 15cm | 이 이상은 점프 |
| 뛰어 오를 수 있는 높이 | 45cm | 의자는 가능, 바닥→책상 직행 불가 |
| 내려올 수 있는 높이 | 1.2m | |
| 설 수 있는 최대 고도 | 바닥 + 1.3m | 천장·벽 중턱 방지 |
| 게임 시작 조건 | 설 수 있는 칸 120개 | |
| 검거 반경 | **3.0m** | |
| 검거 조준각 | 20° | |
| 검거 시간 | 5초 | |
| 게이지 감소 | 초당 12% | 리셋이 아니라 감소 |
| 가려졌을 때 충전 속도 | 정상의 25% | 멈추지 않음 |
| 반투명 정도 | 0.5 | 가려진 동안 |
| 도망 속도 | 0.12 ~ 0.50 m/s | 플레이어가 가까울수록 빠름 |

---

## 6. 남은 문제

**해결하기 어려운 것**

- **유리 통과** — 깊이 센서의 한계다. 완화책은 "플레이어가 걸어간 적 없는 구역 제외" 인데
  아직 넣지 않았다. 플레이어 이동 경로는 이미 기록하고 있으므로 얹기는 어렵지 않다.
- **드리프트 자체** — ARCore 내부에서 일어나고 우리는 결과만 받는다. anchor 는
  드리프트를 막는 것이 아니라 보정에 **편승하는** 장치다.

**아직 안 한 것**

- 궤적이 보정 점프를 실제 이동처럼 그린다. 이동거리 계산에는 이미 상한이 있는데
  궤적 표시에만 빠져 있다. 튄 구간은 끊어서 표시해야 한다.
- 폰이 알려주는 **"지금 위치가 실측인지 추정인지"** 신호를 아직 읽지 않는다.
  읽으면 추적이 나쁠 때 지도 갱신을 멈춰 오염을 예방할 수 있다.
- 운영자 뷰의 복셀 표시가 아직 옛 좌표계라, 보정이 큰 세션에서 통행 타일과 어긋나 보일 수 있다.
  게임 판정에는 영향이 없다.
- 소크 테스트(가상의 방에서 수만 프레임을 빠르게 돌려 불변식을 검사) — 나중으로 미뤘다.
- 색상 의미 충돌: 복셀의 빨강은 "신뢰도 낮음", 통행 타일의 빨강은 "못 감" 이라 사실상 반대다.

**실기기에서 확인이 필요한 것**

- 발판 조건을 4로 올린 뒤 **지도에 구멍이 늘지 않았는지.** "설 수 있는 칸" 숫자가
  전보다 크게 줄었다면 3으로 낮춘다.
- 높이 층을 10cm → 5cm 로 낮출지. 발판 높이 오차가 절반이 되지만
  노이즈 한 점이 "막힘" 을 만들 확률도 올라간다.

---

## 7. 파일 지도

| 파일 | 역할 |
|---|---|
| `v4-chase.html` | 술래잡기 전용 페이지 |
| `index.html` | 팀 모드 선택 화면 |
| `app.html` | 기존 숨바꼭질 게임 |
| `src/traversal-grid.js` | 이동 격자 — 발판·층·이웃 판정 |
| `src/chase-path.js` | 목적지 선택과 경로 탐색 |
| `src/chase-runner.js` | 하츄핑의 이동과 속도 |
| `src/capture-gauge.js` | 검거 규칙과 게이지 |
| `src/line-of-sight.js` | 가림 판정 — 몸에 점 7개를 뿌려 비율로 |
| `src/map-anchor.js` | 원점 anchor 와 좌표 변환 |
| `src/grid-candidates.js` | 동결된 지도에서 숨을 자리 뽑기 |
| `src/chase-log.js` | 도망 기록(블랙박스) |
| `tests/module-syntax.test.mjs` | 브라우저와 같은 방식의 파싱 검사 |

---

## 8. 팀 저장소와의 관계

이 작업은 개인 fork 의 `private/jaehoon` 에 있다. 팀 `main` 에는 명시적으로 지시할 때만 올린다.

팀 `main` 은 그 사이에도 계속 움직이고 있다. 지금까지 받아서 합친 것:

- 은초님 — 키프레임 복셀 복원, 게임용 지형(`VoxelTerrain`), 스캔 JSON 백업, 깊이 버퍼 가림
- 철준님 — 모드 선택 랜딩 페이지(기존 게임이 `app.html` 로 이동)
- `main.js` 괄호 누락 수정

**합칠 때 주의할 점** — 은초님이 지형 누적기를 둘로 나누었으므로(`?terrain=keyframe`),
동결 지도의 "게임 중에는 지도를 건드리지 않는다" 게이트가 **두 경로 모두**를 지나야 한다.
한쪽만 막으면 키프레임 모드에서 동결이 뚫린다.

이 문서 작성 시점에 팀 `main` 에 다시 새 커밋들이 올라와 있다(가위바위보 모바일 개선 등).
다음에 합칠 때 함께 받아야 한다.
